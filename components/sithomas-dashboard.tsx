'use client'

import { FormEvent, useEffect, useMemo, useState } from 'react'
import { Download, Home, LogOut, Menu, Pencil, Plus, Search, ShieldCheck, Trash2, UserRound, Users, X } from 'lucide-react'
import { Button } from '@/components/ui/button'

type Role = 'admin' | 'umat'
const DEFAULT_LOGO = 'https://hebbkx1anhila5yf.public.blob.vercel-storage.com/logo%20sithomas-w3X34u0FmKp7Tfjz87kmqzXaJT728d.png'
type Person = { id: number; No_KK: string; Nama_Lengkap: string; Nama_Baptis: string; Hubungan_Keluarga: string; Agama: string; Status_Perkawinan: string; Pendidikan_Terakhir: string; Pekerjaan: string; Profesi: string }
const seed: Person[] = [
  { id: 1, No_KK: '3273012301010001', Nama_Lengkap: 'Budi Santoso', Nama_Baptis: 'Thomas', Hubungan_Keluarga: 'Kepala Keluarga', Agama: 'Katolik', Status_Perkawinan: 'Menikah', Pendidikan_Terakhir: 'S1', Pekerjaan: 'Karyawan Swasta', Profesi: 'Manajer' },
  { id: 2, No_KK: '3273012301010001', Nama_Lengkap: 'Maria Santoso', Nama_Baptis: 'Maria', Hubungan_Keluarga: 'Istri', Agama: 'Katolik', Status_Perkawinan: 'Menikah', Pendidikan_Terakhir: 'SMA', Pekerjaan: 'Ibu Rumah Tangga', Profesi: '-' },
  { id: 3, No_KK: '3273012301010002', Nama_Lengkap: 'Andreas Wijaya', Nama_Baptis: 'Andreas', Hubungan_Keluarga: 'Kepala Keluarga', Agama: 'Katolik', Status_Perkawinan: 'Menikah', Pendidikan_Terakhir: 'SMA', Pekerjaan: 'Wiraswasta', Profesi: 'Pemilik Toko' },
  { id: 4, No_KK: '3273012301010002', Nama_Lengkap: 'Clara Wijaya', Nama_Baptis: 'Clara', Hubungan_Keluarga: 'Anak', Agama: 'Katolik', Status_Perkawinan: 'Belum Menikah', Pendidikan_Terakhir: 'S1', Pekerjaan: 'Mahasiswa', Profesi: '-' },
]
const fields: (keyof Omit<Person, 'id'>)[] = ['No_KK', 'Nama_Lengkap', 'Nama_Baptis', 'Hubungan_Keluarga', 'Agama', 'Status_Perkawinan', 'Pendidikan_Terakhir', 'Pekerjaan', 'Profesi']
const blank = (kk = ''): Omit<Person, 'id'> => ({ No_KK: kk, Nama_Lengkap: '', Nama_Baptis: '', Hubungan_Keluarga: 'Anggota Keluarga', Agama: 'Katolik', Status_Perkawinan: 'Menikah', Pendidikan_Terakhir: 'SMA', Pekerjaan: '', Profesi: '' })
const dropdownOptions: Partial<Record<keyof Omit<Person, 'id'>, string[]>> = {
  Hubungan_Keluarga: ['Kepala Keluarga', 'Istri', 'Suami', 'Anak', 'Orang Tua', 'Saudara', 'Anggota Keluarga'],
  Agama: ['Katolik', 'Kristen Protestan', 'Islam', 'Hindu', 'Buddha', 'Konghucu', 'Lainnya'],
  Status_Perkawinan: ['Belum Menikah', 'Menikah', 'Cerai Hidup', 'Cerai Mati'],
  Pendidikan_Terakhir: ['Tidak/Belum Sekolah', 'SD', 'SMP', 'SMA', 'D1/D2/D3', 'S1', 'S2', 'S3'],
  Pekerjaan: ['Pelajar', 'Mahasiswa', 'Karyawan Swasta', 'Pegawai Negeri', 'Wiraswasta', 'Ibu Rumah Tangga', 'Pensiunan', 'Tidak Bekerja', 'Lainnya'],
  Profesi: ['Pelajar/Mahasiswa', 'Guru/Dosen', 'Tenaga Kesehatan', 'Pengusaha', 'Manajer', 'Karyawan', 'Teknisi', 'Petani', 'Pensiunan', 'Lainnya'],
}

export function SithomasDashboard({ logoUrl }: { logoUrl?: string }) {
  const [people, setPeople] = useState(seed), [role, setRole] = useState<Role | null>(null), [mode, setMode] = useState<Role>('admin'), [credential, setCredential] = useState(''), [password, setPassword] = useState(''), [familyKK, setFamilyKK] = useState(''), [query, setQuery] = useState(''), [section, setSection] = useState<'ringkasan' | 'keluarga' | 'anggota'>('ringkasan'), [modal, setModal] = useState(false), [editing, setEditing] = useState<Person | null>(null), [form, setForm] = useState(blank()), [mobileNav, setMobileNav] = useState(false), [error, setError] = useState(''), [hydrated, setHydrated] = useState(false)
  useEffect(() => {
    try {
      const saved = window.localStorage.getItem('sithomas-people')
      if (saved) setPeople(JSON.parse(saved) as Person[])
    } catch { /* use seeded data when device storage is unavailable */ }
    setHydrated(true)
  }, [])
  useEffect(() => {
    if (hydrated) window.localStorage.setItem('sithomas-people', JSON.stringify(people))
  }, [hydrated, people])
  const visible = useMemo(() => people.filter((p) => (role === 'admin' || p.No_KK === familyKK) && (section !== 'keluarga' || p.Hubungan_Keluarga === 'Kepala Keluarga') && Object.values(p).join(' ').toLowerCase().includes(query.toLowerCase())), [people, role, familyKK, query, section])
  const families = new Set(people.map((p) => p.No_KK)).size
  function login(e: FormEvent) { e.preventDefault(); setError(''); if (mode === 'admin' && credential.trim() === 'admin' && password === 'admin123') return setRole('admin'); if (mode === 'umat' && /^\d{4}$/.test(credential.trim())) { const match = people.find((p) => p.No_KK.endsWith(credential.trim())); if (match) { setFamilyKK(match.No_KK); return setRole('umat') } } setError(mode === 'umat' ? 'Nomor KK tidak ditemukan. Coba 0001 atau 0002.' : 'Username atau password salah.') }
  function openForm(person?: Person) { setEditing(person ?? null); setForm(person ? (({ id: _id, ...rest }) => rest)(person) : blank(role === 'umat' ? familyKK : '')); setModal(true) }
  function save(e: FormEvent) { e.preventDefault(); if (!form.No_KK || !form.Nama_Lengkap) return; if (role === 'umat' && form.No_KK !== familyKK) return; setPeople((current) => editing ? current.map((p) => p.id === editing.id ? { ...form, id: editing.id } : p) : [{ ...form, id: Date.now() }, ...current]); setModal(false) }
  function remove(id: number) { if (window.confirm('Hapus data umat ini?')) setPeople((current) => current.filter((p) => p.id !== id)) }
  function exportCsv() { const header = fields.join(','); const rows = people.map((p) => fields.map((f) => `"${String(p[f]).replaceAll('"', '""')}"`).join(',')); const blob = new Blob(['\ufeff' + [header, ...rows].join('\n')], { type: 'text/csv;charset=utf-8;' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = 'data-umat-st-thomas.csv'; a.click(); URL.revokeObjectURL(url) }
  if (!role) return <Login logoUrl={logoUrl} mode={mode} setMode={(m) => { setMode(m); setCredential(''); setPassword(''); setError('') }} credential={credential} setCredential={setCredential} password={password} setPassword={setPassword} error={error} onSubmit={login} />
  return <main className="min-h-screen bg-background"><div className="flex min-h-screen"><aside className={`${mobileNav ? 'fixed inset-0 z-40 flex' : 'hidden'} w-72 shrink-0 flex-col border-r border-border bg-sidebar md:relative md:flex`}><div className="flex items-center justify-between px-6 py-7"><Brand logoUrl={logoUrl} /><button className="md:hidden" onClick={() => setMobileNav(false)} aria-label="Tutup menu"><X /></button></div><nav className="flex flex-col gap-2 px-4">{([['ringkasan', Home, 'Ringkasan umat'], ['keluarga', Users, 'Data keluarga'], ['anggota', UserRound, 'Data anggota']] as const).map(([key, Icon, label]) => <button key={key} onClick={() => { setSection(key); setMobileNav(false) }} className={`flex items-center gap-3 rounded-xl px-4 py-3 text-left text-sm font-bold ${section === key ? 'bg-primary text-primary-foreground' : 'text-sidebar-foreground hover:bg-sidebar-accent'}`}><Icon />{label}</button>)}</nav><div className="mt-auto border-t border-sidebar-border p-5"><div className="flex items-center gap-3"><div className="grid size-10 place-items-center rounded-full bg-accent font-bold text-primary">{role === 'admin' ? 'AD' : 'KK'}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{role === 'admin' ? 'Admin Utama' : 'Kepala Keluarga'}</p><p className="text-xs text-muted-foreground">{role === 'admin' ? 'Akses semua data' : `KK ...${familyKK.slice(-4)}`}</p></div><button onClick={() => { setRole(null); setSection('ringkasan') }} aria-label="Keluar"><LogOut className="text-muted-foreground" /></button></div></div></aside><section className="min-w-0 flex-1"><header className="flex items-center justify-between border-b border-border bg-card px-5 py-4 md:px-8"><button className="md:hidden" onClick={() => setMobileNav(true)} aria-label="Buka menu"><Menu /></button><div className="hidden md:block"><p className="text-xs font-bold uppercase tracking-widest text-primary">{role === 'admin' ? 'Mode admin utama' : 'Mode kepala keluarga'}</p><h1 className="font-serif text-xl font-bold">Pendataan Umat</h1></div><div className="flex items-center gap-3"><div className="hidden text-right sm:block"><p className="text-sm font-bold">St. Thomas Stasi Sampangan</p><p className="text-xs text-muted-foreground">Data aktif</p></div><div className="grid size-10 place-items-center rounded-full bg-primary text-sm font-bold text-primary-foreground">{role === 'admin' ? 'A' : 'K'}</div></div></header><div className="mx-auto max-w-7xl p-5 md:p-8"><div className="mb-8 flex flex-col justify-between gap-5 lg:flex-row lg:items-end"><div><p className="mb-2 text-sm font-bold text-primary">Selamat datang di SITHOMAS</p><h2 className="font-serif text-3xl font-bold tracking-tight md:text-4xl">Pusat data umat yang terpercaya.</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Kelola data keluarga dan anggota umat Lingkungan St. Thomas dengan rapi, aman, dan mudah digunakan.</p></div><div className="flex flex-wrap gap-3">{role === 'admin' && <Button variant="outline" onClick={exportCsv} className="rounded-xl"><Download data-icon="inline-start" /> Simpan CSV</Button>}<Button onClick={() => openForm()} className="rounded-xl"><Plus data-icon="inline-start" /> {role === 'admin' ? 'Tambah umat' : 'Tambah anggota'}</Button></div></div><div className="grid gap-4 sm:grid-cols-3"><Stat label="Total umat" value={String(visible.length)} icon={Users} /><Stat label="Keluarga" value={String(families)} icon={Home} /><Stat label="Mode akses" value={role === 'admin' ? 'Admin' : 'Keluarga'} icon={ShieldCheck} /></div><div className="mt-8 rounded-2xl border border-border bg-card shadow-sm"><div className="flex flex-col gap-4 border-b border-border p-5 md:flex-row md:items-center md:justify-between"><div><h3 className="font-serif text-xl font-bold">{section === 'keluarga' ? 'Data keluarga' : 'Data anggota umat'}</h3><p className="mt-1 text-sm text-muted-foreground">{role === 'admin' ? 'Seluruh data umat lingkungan' : `Data keluarga dengan KK ...${familyKK.slice(-4)}`}</p></div><div className="relative w-full md:w-72"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground" /><input aria-label="Cari data" value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Cari nama atau No. KK" className="h-10 w-full rounded-xl border border-input bg-background pl-10 pr-3 text-sm outline-none focus:ring-2 focus:ring-ring" /></div></div><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-muted text-xs uppercase tracking-wide text-muted-foreground"><tr>{['Nama lengkap', 'No. KK', 'Hubungan', 'Agama', 'Pendidikan', 'Pekerjaan', 'Aksi'].map((h) => <th key={h} className="px-5 py-4 font-bold">{h}</th>)}</tr></thead><tbody className="divide-y divide-border">{visible.map((p) => <tr key={p.id} className="hover:bg-muted/40"><td className="px-5 py-4"><p className="font-bold">{p.Nama_Lengkap}</p><p className="text-xs text-muted-foreground">{p.Nama_Baptis || 'Belum diisi'}</p></td><td className="px-5 py-4 font-mono text-xs">{p.No_KK}</td><td className="px-5 py-4">{p.Hubungan_Keluarga}</td><td className="px-5 py-4">{p.Agama}</td><td className="px-5 py-4">{p.Pendidikan_Terakhir}</td><td className="px-5 py-4">{p.Pekerjaan || '-'}</td><td className="px-5 py-4"><div className="flex gap-2"><button onClick={() => openForm(p)} className="grid size-9 place-items-center rounded-lg border border-border text-primary hover:bg-accent" aria-label={`Edit ${p.Nama_Lengkap}`}><Pencil /></button>{role === 'admin' && <button onClick={() => remove(p.id)} className="grid size-9 place-items-center rounded-lg border border-border text-destructive hover:bg-destructive/10" aria-label={`Hapus ${p.Nama_Lengkap}`}><Trash2 /></button>}</div></td></tr>)}</tbody></table>{visible.length === 0 && <p className="p-10 text-center text-sm text-muted-foreground">Data tidak ditemukan.</p>}</div></div></div></section></div>{modal && <Modal editing={editing} form={form} setForm={setForm} onClose={() => setModal(false)} onSave={save} role={role} />}</main>
}
function Brand({ logoUrl }: { logoUrl?: string }) { const [failed, setFailed] = useState(false); return <div className="flex items-center gap-3"><div className="grid size-12 shrink-0 place-items-center overflow-hidden"><img src={failed ? DEFAULT_LOGO : (logoUrl || DEFAULT_LOGO)} alt="Logo SITHOMAS" className="size-full object-contain" onError={() => setFailed(true)} />{failed && <ShieldCheck className="text-primary" />}</div><div><p className="font-serif text-xl font-bold">SITHOMAS</p><p className="text-[10px] uppercase tracking-[.18em] text-muted-foreground">St. Thomas Stasi</p></div></div> }
function Login({ logoUrl, mode, setMode, credential, setCredential, password, setPassword, error, onSubmit }: { logoUrl?: string; mode: Role; setMode: (v: Role) => void; credential: string; setCredential: (v: string) => void; password: string; setPassword: (v: string) => void; error: string; onSubmit: (e: FormEvent) => void }) { const [failed, setFailed] = useState(false); return <main className="grid min-h-screen place-items-center bg-background px-5 py-10"><div className="w-full max-w-md"><div className="mb-8 text-center"><div className="mx-auto mb-4 grid size-28 place-items-center overflow-hidden"><img src={failed ? DEFAULT_LOGO : (logoUrl || DEFAULT_LOGO)} alt="Logo SITHOMAS" className="size-full object-contain" onError={() => setFailed(true)} />{failed && <ShieldCheck className="size-10 text-primary" />}</div><p className="font-serif text-3xl font-bold">SITHOMAS</p><p className="mt-1 text-xs font-bold uppercase tracking-[.24em] text-primary">St. Thomas Stasi Sampangan</p></div><div className="rounded-3xl border border-border bg-card p-6 shadow-xl md:p-8"><h1 className="font-serif text-2xl font-bold">Masuk ke pendataan umat</h1><p className="mt-2 text-sm leading-6 text-muted-foreground">Pilih akses sesuai peran Anda.</p><div className="mt-6 grid grid-cols-2 rounded-xl bg-muted p-1"><button type="button" onClick={() => setMode('admin')} className={`rounded-lg py-2.5 text-sm font-bold ${mode === 'admin' ? 'bg-card text-primary shadow-sm' : 'text-muted-foreground'}`}>Admin utama</button><button type="button" onClick={() => setMode('umat')} className={`rounded-lg py-2.5 text-sm font-bold ${mode === 'umat' ? 'bg-card text-primary shadow-sm' : 'text-muted-foreground'}`}>Kepala keluarga</button></div><form onSubmit={onSubmit} className="mt-6 flex flex-col gap-4"><label className="flex flex-col gap-2 text-sm font-bold">{mode === 'admin' ? 'Username' : '4 angka terakhir No. KK'}<input required inputMode={mode === 'umat' ? 'numeric' : 'text'} maxLength={mode === 'umat' ? 4 : undefined} value={credential} onChange={(e) => setCredential(e.target.value)} className="h-11 rounded-xl border border-input bg-background px-3 font-normal outline-none focus:ring-2 focus:ring-ring" placeholder={mode === 'admin' ? 'admin' : '0001'} /></label>{mode === 'admin' && <label className="flex flex-col gap-2 text-sm font-bold">Password<input required type="password" value={password} onChange={(e) => setPassword(e.target.value)} className="h-11 rounded-xl border border-input bg-background px-3 font-normal outline-none focus:ring-2 focus:ring-ring" placeholder="admin123" /></label>}{error && <p className="rounded-xl bg-destructive/10 p-3 text-sm text-destructive" role="alert">{error}</p>}<Button type="submit" className="h-11 rounded-xl">Masuk</Button></form><p className="mt-5 text-center text-xs text-muted-foreground">Admin: admin / admin123 · KK demo: 0001 atau 0002</p></div></div></main> }
function Stat({ label, value, icon: Icon }: { label: string; value: string; icon: typeof Users }) { return <div className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="grid size-10 place-items-center rounded-xl bg-accent text-primary"><Icon /></div><p className="mt-5 text-sm text-muted-foreground">{label}</p><p className="mt-1 font-serif text-3xl font-bold">{value}</p></div> }
function Modal({ editing, form, setForm, onClose, onSave, role }: { editing: Person | null; form: Omit<Person, 'id'>; setForm: (v: Omit<Person, 'id'>) => void; onClose: () => void; onSave: (e: FormEvent) => void; role: Role }) { return <div className="fixed inset-0 z-50 grid place-items-center bg-foreground/50 p-4" role="dialog" aria-modal="true"><div className="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-2xl border border-border bg-card p-6 shadow-2xl"><div className="flex items-start justify-between"><div><h2 className="font-serif text-2xl font-bold">{editing ? 'Edit data umat' : role === 'umat' ? 'Tambah anggota keluarga' : 'Tambah data umat'}</h2><p className="mt-1 text-sm text-muted-foreground">Lengkapi data umat Lingkungan St. Thomas.</p></div><button onClick={onClose} aria-label="Tutup"><X className="text-muted-foreground" /></button></div><form onSubmit={onSave} className="mt-6 grid gap-4 sm:grid-cols-2">{fields.map((field) => <label key={field} className="flex flex-col gap-2 text-sm font-bold">{field.replaceAll('_', ' ')}{dropdownOptions[field] ? <select required={field !== 'Profesi'} disabled={role === 'umat' && field === 'No_KK'} value={form[field]} onChange={(e) => setForm({ ...form, [field]: e.target.value })} className="h-11 rounded-xl border border-input bg-background px-3 font-normal outline-none focus:ring-2 focus:ring-ring disabled:bg-muted">{dropdownOptions[field]?.map((option) => <option key={option} value={option}>{option}</option>)}</select> : <input required={field === 'No_KK' || field === 'Nama_Lengkap'} readOnly={role === 'umat' && field === 'No_KK'} value={form[field]} onChange={(e) => setForm({ ...form, [field]: e.target.value })} className="h-11 rounded-xl border border-input bg-background px-3 font-normal outline-none focus:ring-2 focus:ring-ring read-only:bg-muted" />}</label>)}<div className="flex justify-end gap-3 sm:col-span-2"><Button type="button" variant="outline" onClick={onClose} className="rounded-xl">Batal</Button><Button type="submit" className="rounded-xl">Simpan data</Button></div></form></div></div> }
